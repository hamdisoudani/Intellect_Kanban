import { handlers } from "@intellect-kanban/utils";

/**
 * Export the GET and POST handlers for NextAuth
 * These route handlers are automatically called by NextAuth.js
 */
export const { GET, POST } = handlers; 